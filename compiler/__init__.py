"""Siyur compiler — the offline-bundle pipeline.

pmtiles extract + Valhalla walking legs + license quarantine + ``BundleManifestV1``
(content-hashed, verifiable). Nothing with ``bundleable=false`` may enter a bundle.

Skeleton stage (DU-00): package exists and imports; pipeline lands in M1 (DU-05).
"""

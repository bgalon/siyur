{
  "name": "siyur-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Siyur PWA — Vite + MapLibre + Workbox. DU-00 walking-skeleton slice: an empty MapLibre map renders and the app shell precaches offline. Config mirrors the ADR-0003 spike so the DU-06 OPFS/PMTiles worker slots in unchanged.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "maplibre-gl": "~5.19.0"
  },
  "devDependencies": {
    "@types/node": "~22.10.2",
    "jsdom": "~25.0.1",
    "typescript": "~5.6.3",
    "vite": "8.1.5",
    "vite-plugin-pwa": "1.3.0",
    "vitest": "~4.1.10",
    "workbox-build": "7.4.1",
    "workbox-window": "7.4.1"
  }
}

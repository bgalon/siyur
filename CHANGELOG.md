# Changelog

Generated by [git-cliff](https://git-cliff.org) from conventional commits.
Do not hand-edit — run `git-cliff -o CHANGELOG.md`.

## Unreleased

### Build

- Initialise Alembic against SIYUR_DATABASE_URL (T003)
- Add docker-compose PostGIS service (T002)
- Pin slice-001 dependencies (T001)
- Scaffold web/ Vite PWA rendering an empty MapLibre map
- Planner-seam + eval deps, package AGENTS.md stubs, geo tripwire (unit d)
- Toolchain + empty package skeleton (unit a)

### CI

- Exclude canonical licence texts from the diff-guard count (#103)
- Exclude captured fixtures + alembic scaffold from diff-guard
- Real PostGIS service container on job 3 (T006)
- Run real web typecheck + vitest (replace echo stubs)
- Fix security PR-commit perms + diff-guard live-label read
- Stand up 7-job CI gate green on stubs + security-driven pin bumps

### Chores

- Ratify six ADRs, add the FAIL-009 merge guard, resync tasks.md (#107)
- Notice unwritten devlog entries at session start
- Close the Bash secret-read hole, gate agent self-edits
- Enable auto mode + Agent Teams, add three subagents
- Log permission telemetry, consolidate 177 rules to 103
- ADR-0006 — narrow CI workflow-edit deny to ask
- Introduce PR + worktree/cloud parallel workflow (ADR-0005)
- Allow python -m py_compile in shared settings
- D0 agent-instruction bootstrap (governance-first)
- Seed repo from planning session (2026-07-24)

### Docs, decisions & course artifacts

- 2026-08-14, and stop the debt hook flagging days already covered (#117)
- 2026-08-11 — the day the repo went public, and what that fixed (#108)
- ADR-0030 — attribution scope, sourced values chip and user composition is captioned (#93)
- Licence the repository, three ways, before going public (#102)
- Tick the 16 tasks that shipped but were never checked off
- 2026-08-08 — narration, and reasoned-vs-run verification (#91)
- Ratify ADR-0020, 0021, 0024 and 0027 (Ben, 2026-08-08)
- Ratify ADR-0014..0019, amend ADR-0022, add ADR-0029, and a guardrail for FAIL-008 (#82)
- Reconcile the design authority before any DU-04 code (T007/T007a/T007b)
- Clear the non-blocking review findings (6-16) and nits
- Corrections from the card-amendment and review pass
- Spec 002 tasks, ADR-0020..0025, and the schema-card amendments they force
- Spec 002 Phase 0 research; correct the quickstart's auth
- Spec 002 Phase 1 — data model, contracts, quickstart
- Spec 002 implementation plan
- Spec 002 — plan a day, compile it, travel it offline
- Close the instruction gaps the event trail exposed
- Reconstruct 2026-08-06 from the captured event trail
- Restore the changelog generator and log the 2026-08-07 session
- ADR-0019 — attribution is co-present with its value, not permanent
- Record the four decisions US1 flagged but did not settle (0015-0018)
- Record the pip-audit CVE that reddened job 6 after the gate check
- Re-aim quickstart at acceptance, record the T069 gate verification
- Close Spec 001 US1 — devlog, task markup, honest TRY-IT (T070)
- FAIL-005 cross-engine string normalization + class guardrail
- Audit poi-site card vs slice 001, add area card + prompt drift guard
- Prompt registry v1 + ADR-0014 (research makes no model call)
- Correct the worktree expectation in TRY-IT.md
- Session status, FAIL-004, and a verified TRY-IT guide
- Ratify three open decisions (ADR-0012, ADR-0013, ADR-0010 amendment)
- Tasks.md for Spec 001 + widen agent permissions for parallel sessions
- Ratify ADR-0009/0010, add ADR-0011 (Authlib SSO), devlog 2026-07-31
- Implementation plan for Spec 001 (research→cited-commons→map)
- Spec 001 — research an area into a cited commons on the map (clarified)
- Record branch-protection is self-enforced (free-tier solo repo)
- Ratify ADR-0007 (accepted, approved-by Ben)
- ADR-0007 + FAIL-003 + devlog for unit d (package skeleton)
- DU-00 unit c — data-spine schema cards + DATA-LICENSES
- Five docs/data schema cards + DATA-LICENSES registry (DU-00 unit c)
- DU-00 unit b — Spec Kit init + constitution ratified
- Ratify Siyur constitution v1.0.0 + Spec Kit init (DU-00 unit b)
- DU-00 kickoff devlog + FAIL-002 (ruff autofix broke 3.9 hook)
- Record the ADR-0003 Vite spike execution
- Ratify DU-04 research-tier routing (ADR-0004 option 1)
- Reconcile plan with executed D5 spikes + ADR-0005 (v1.1)
- Pin Vite 8.1.5 / vite-plugin-pwa 1.3.0 / Workbox 7.4.1 (ADR-0003 spike)
- ADR-0004 Confirmation satisfied by executed planner spike
- Accept ADRs 0002–0004 (offline-first, Vite, layered planner) + reconcile docs
- Add Visual Language reference to the UX handoff
- Import UX handoff ("The Field Atlas") + integration notes
- Record M1 transliteration-sliver decision (accepted)
- D5 — fold discovery-spike findings into the design
- D2 — delivery-plan.md (PRD split into deliverable units)
- D4 — agent-ops.md (continuously-evolving-agents model)
- D3 — test-strategy.md (three tiers + evals axis + CI gates)
- D1 — tech-design v1.0 (M1-slice-deep)
- Add Claude-design brief for the UX track
- ADR-0001 + first devlog entry for the D0 bootstrap
- Revise to v2.0 — multi-user research platform

### Features

- The bundles endpoints, and the two dead ends in the plan lifecycle (T043, T047) (#115)
- The compile pipeline, and the approved day is frozen not re-routed (T042) (#114)
- The basemap stage, glyph/sprite integrity, and the licences it exposed (T035, T035a, T035b) (#113)
- The walking network and its nodedness tripwire (T036, T037) (#112)
- The plans API, the wiring that makes the gate reachable, and the frame POST /areas dropped (T024, T025) (#110)
- Run_plan streams status, itinerary, feasibility, done (T023) (#105)
- The per-bundle attribution file (T039, T046) (#100)
- Bundle object storage with real range semantics (T041) (#99)
- The bundle manifest — per-artifact hashes and the frozen frame (T040, T040a, T045) (#98)
- The plan review surface, with approve unattached rather than disabled (T027, T028) (#101)
- The HITL approval gate as row state, not a suspended coroutine (T022, T026) (#97)
- A feasibility verdict and a proposal node that cannot emit a numeric (T018-T021) (#96)
- The licence quarantine filter, with its traps pinned (T038, T044) (#95)
- The RoutingProvider seam — Valhalla, ORS fallback, fixtures (T016, T017)
- The offline travel UI and off-route recovery (T051, T052)
- The offline bundle transport — OPFS, module worker, integrity (T048-T050, T053)
- The area's local frame, and the HITL gate's table (T008, T009)
- The narrate node and its prompt registry entry (T032, T034) (#86)
- The Wikivoyage/Wikipedia narration adapter, with its traps pinned (T031, T033) (#85)
- The opening-hours evaluator, fail-closed on three inputs (T014, T015) (#84)
- The DU-04 data spine — ItineraryV1, RouteLegV1, BundleManifestV1 (T010-T013)
- Phase-1 setup and the airplane-mode gate, made real (T001-T004, T029, T030)
- Allowlist MIT as a bundleable data license (ADR-0026)
- A dev vector basemap so the map shows streets
- One command to start and stop the dev stack
- Enforce ledger idempotence in the database, not in Python
- Frame the researched area and give the dev server a backend
- Proxy the API through the dev server so the map has a backend
- Delimit an area and stream its research pass (US1)
- Areas, research SSE and sites endpoints (T037–T041, T050)
- Reuse a covered area instead of re-researching it (T052/T062)
- Stream resolve_area → research → curate → persist (T035/T036)
- Land merged records in PostGIS — upsert, coverage, cited reads
- Resolve an area name/bbox/polygon to one stamped polygon (T032)
- Research fan-out + curate merge/translit/rank nodes (T033/T034/T059)
- PostGIS schema + initial migration (T014/T015)
- Script guard + deterministic Greek→Latin transliteration
- Cluster candidate records into places and merge them (T028)
- Merge fields per-source with conflicts and a provenance ledger (T029)
- Add the merge join rule — gers_id, else ε=25 m AND τ=0.6 (T028)
- OSM Overpass adapter with graceful degradation (T024, T025, T027)
- Overture places adapter over DuckDB parquet (T023, T026)
- Add the SourceAdapter boundary protocol (T022)
- ModelRouter seam with pinned tiers and capability gate (T018/T019)
- Cited-sites map layer with per-value attribution chips (T042–T044)
- Add SiteRecordV1 and make unstamped values unconstructible (T007, T008)
- Add EPSG:4326 point validation helpers (T010)
- Add the license quarantine allowlist (T009)
- Google SSO login via Authlib + /healthz and /me (DU-00 runtime slice)

### Fixes

- The Tier-2 harness gets its own database, never yours (FAIL-011) (#116)
- Discover routing captures by shape, not by place name
- RFC 8785 canonicalization, the missing checked_at column, and a CHECK got wrong twice
- Pin image digests, correct the timezonefinder claim and the SH design
- Overpass mirror + a retry policy that survives a rate limit
- Make the sprite URL absolute, as MapLibre requires (FAIL-007)
- The five blocking defects from the Spec 002 review
- Covered means "researched", not "contains a site"
- Give an unnamed marker an accessible name without inventing one
- Static dedupe SQL in 0003, no dynamic construction
- Render dense site markers as dots, peek attribution on demand
- Constrain cryptography>=50.0.0 for PYSEC-2026-3552
- Grow the ranking prompt past Sonnet 5's cacheable-prefix floor
- Derive resolve_area's Python and SQL folds from one definition
- Dedupe conflicts across both groups on refresh (T051)
- Satisfy mypy strict in scripts/permission_report.py
- Let `und` names match tagged names so cross-source joins work

### Performance

- Stop the divisions name lookup reading the whole global theme

### Refactoring

- Keep guards.ts helper predicates module-private

### Style

- Trim caching eval under the 500-line diff guard

### Tests

- Live Valhalla and MediaWiki fixtures (T005, T006) (#83)
- Drop a redundant natural-key case
- Prove genericity on a second, non-Greek-script area (T063)
- Re-aim T065 caching eval at the curate tier (ADR-0014)
- Merge-blocking story evals for SC-002, FR-005 and FAIL-001
- Tier-2 PostGIS harness + persistence round-trip (T016/T017)
- Cover the transliteration sliver and the FAIL-001 guard
- AST seam-purity tripwire + offline router tests (T020/T021)
- Contract-mocked tests for markers, chips and ODbL control (T045)
- Unit + hypothesis tests for the data spine (T011, T012, T013)
- Commit real Overture + Overpass fixtures for Rhodes (T004, T005)


